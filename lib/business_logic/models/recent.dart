class Recent {
  String bookID;
  int pageNumber;
  String bookName;

  Recent(this.bookID, this.pageNumber,[this.bookName]);

  // Map<String, dynamic> toMap() {
  //   return {COLUMN_BOOK_ID: this.bookID, COLUMN_PAGE_NUMBER: this.pageNumber};
  // }

  // Recent.fromMap(Map<String, dynamic> map) {
  //   this.bookID = map[COLUMN_BOOK_ID];
  //   this.pageNumber = map[COLUMN_PAGE_NUMBER];
  //   this.bookName = map[COLUMN_NAME];
  // }


}
